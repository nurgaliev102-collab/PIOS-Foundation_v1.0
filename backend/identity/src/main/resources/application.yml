spring:
  application:
    name: identity
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/pios_identity
    username: postgres
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration/identity

server:
  port: 8086
