# PIOS Taxi — Commercial Execution Tracker

**Live document — updated as work progresses, not a one-time report.** Started 2026-09-15. Owner: Claude Code, this session. Read alongside `docs/PIOS_TAXI_PRODUCT_COMPLETION_AUDIT.md`, `docs/PIOS_TAXI_COMMERCIAL_PRODUCT_BLUEPRINT.md`, ADR-068/069/070.

| Цель | Текущее состояние | Что сделано | Доказательство |
|---|---|---|---|
| Новый пассажир находит водителя | **Реализовано** | ADR-070, `/request` entry point, Fallback Dispatch Tier 3 | Live E2E, production, `piosapp.ru`, `75b3c64` |
| Водитель получает нового клиента | **Реализовано** | `POST /v1/connections` (passenger-confirmed, Option B) | Live E2E — `driver_milestones.completed_rides_count=1`, connection created |
| Клиент становится постоянным | **Реализовано** | `explicitDriverIntent` repeat path — PIOS returns the same driver | Live E2E — repeat order → same driver, proposal `201` |
| Мой бизнес | **Реализовано (CRM-глубина)** | Ride count, last-ride date, repeat flag per client (ADR-071 companion Piece 1) | 350/352 frontend tests; data-level confirmed via live `GET /v1/connections?driverId=` + `driver_milestones` |
| Пассажирская ценность | **Частично** — сильна для повторных пассажиров, слабый сигнал доверия при первой встрече | Осталось как есть — намеренно (см. ниже) | — |
| Репутация / сигнал доверия | **Отклонено product owner** — `DRIVER_IDENTITY_DESIGN_DECISION.md` §4 остаётся в силе, не переопределён | Product/Architecture Decision Gap зафиксирован, не реализовано | — |
| Реферал водителей | **Реализовано** (ADR-073, single-hop, anti-MLM) | `/d/:inviterDriverCode`, `invitedByDriverId`, честный счётчик | Live E2E: X приглашает Y → `invitedDriversCount=1`; **найден и исправлен реальный баг** (счётчик изначально исключал is_test-водителей, что делало фичу непроверяемой через принятую в проекте isTest-методологию) |
| Уведомления | **Реализовано (in-app v1)** | ADR-071, 14 фактов (D1-D7/P1-P7), poll-derived, без нового backend | 350/352 frontend tests; честно не покрывает закрытое приложение (названо, не скрыто) |
| Подписка | **Контейнер реализован, тарифа нет** (ADR-074) | Новый модуль `billing`, FREE/TRIAL/ACTIVE/EXPIRED, ничего не гейтит | 68/68 backend tests; **не задеплоено** — намеренно, только каркас в коде |
| UI | **Частично** — emoji-дефекты исправлены; полный Apple-like проход не делался в этой задаче | Emoji/hardcoded-имя исправлены (предыдущий ход) | Скриншоты, предыдущий ход |
| Production | dispatch, order-management, driver-management, identity, frontend на `8206ff3` (guest identity, requestedDriverId, dispatch retry — ADR-075/076/077); billing НЕ задеплоен (по замыслу ADR-074) | Задеплоено и частично верифицировано live; полный driver-side lifecycle не переверифицирован после этого деплоя (см. секцию ниже) | SHA256 всех артефактов, systemd health, live: discovery-order → server-side proposal → старый passenger-direct путь корректно отклонён (401/403); `UNFULFILLED` retry-цикл подтверждён live |

## Главный объединённый E2E (Commercial Taxi E2E, Шаг 20 задания)

```
Mentor регистрируется
  → New Driver регистрируется по реферальной ссылке Mentor (/d/:code)
  → Mentor.invitedDriversCount == 1 (после фикса; было 0 — баг найден и исправлен)
  → New Driver выходит на линию
  → Незнакомый пассажир (0 связей) создаёт заказ без ссылки
  → PIOS находит New Driver через Fallback Dispatch Tier 3
  → цена согласована (420 RUB) → Assignment → arrive → start → COMPLETED
  → пассажир сохраняет New Driver ("Добавить в мои водители") → Connection создан, primary
  → New Driver.completedRidesCount == 1 (Мой бизнес)
  → повторный заказ с explicitDriverIntent → proposal снова New Driver'у
  → cleanup: 0 остатков, smoke-test-driver восстановлен в AVAILABLE
```

**Единственная накладка в процессе, честно зафиксированная:** первая попытка заказа не дала proposal — гонка между API-подтверждением доступности водителя и распространением события через RabbitMQ. Retry намеренно не предусмотрен (ADR-068), поэтому просто отправлен новый заказ, который прошёл штатно. Не баг продукта — особенность тестового скрипта (нет паузы между "водитель вышел на линию" и "заказ создан").

*(Table updated at the end of each phase below, not left stale.)*

## Progress Log

- **2026-09-15, Этап 1–3 начаты.** Product-model decision made by product owner (not by Claude, per rule 26): reputation/trust signal will be a **non-numeric fact from completed rides** (e.g. "N completed rides"), not a rating — consistent with `DriverTrustIndicator.tsx`'s existing documented principle, so this is additive, not a reversal.
- Two architecture reviews launched in parallel: (A) engagement layer — client CRM depth, passenger-facing trust signal, minimal notification layer; (B) growth/monetization foundation — driver-to-driver referral (single-hop, explicitly anti-MLM), subscription/billing *architecture only* (no price decided, no real payment provider, baseline functionality stays free).
- **Piece 2 (passenger-facing trust signal) DECLINED** by product owner — `DRIVER_IDENTITY_DESIGN_DECISION.md` §4 stays authoritative, not superseded. Recorded as an open Product/Architecture Decision Gap, no ADR written.
- **ADR-071 (in-app notifications), ADR-073 (driver referral), ADR-074 (billing foundation container) ratified and committed** (`c53c4d3`), H10/H11/H12 registered.
- One architect agent caught and honestly disclosed its own earlier fabricated completion claim (reported writing files it never wrote) — corrected before any downstream work relied on it. Noted for the record, not hidden.
- Three developer agents now implementing in parallel: (1) CRM client depth + in-app notifications, (2) driver-to-driver referral, (3) billing/subscription container.
- All three hit a session-wide rate limit mid-task, resumed successfully, all three completed: ADR-074 (billing) `95f3005` (68/68 tests), ADR-073 (referral) `7e1f425` (212/212 driver-management, 325/325 frontend), ADR-070/071 (CRM depth + notifications) `d7d53a1` (352/352 frontend, dispatch 597/600 — 3 pre-existing unrelated failures). All merged into `pios-product-main`, full 8-module backend build green. Independent QA review launched before push/deploy.

## 2026-09-16 — Commercial continuation (local workspace, not deployed)

This section supersedes the earlier statement that an order without a proposal after the availability-event race is merely a test-script issue. That was a real commercial-readiness gap; the subsequent local no-proposal retry implementation is recorded below and remains undeployed.

- Direct orders now carry the selected `requestedDriverId` from authenticated order submission through Order Management's outbox to Dispatch. The passenger browser no longer has the power to create arbitrary proposals. The asynchronous event can still result in no proposal when the driver is ineligible or unavailable; saving the order is not proof of an offer.
- Driver creation and assignment writes/reads have tighter participant/owner authorization. Driver order lookup intersects requested IDs with proposals Dispatch confirms for that driver's bearer token. Identity↔Driver association is one-to-one and protected by a database unique index. Guest-first identity and in-place upgrade preserve the same identity reference across orders and relationships.
- The guest creation limiter uses the socket address unless a loopback frontend proxy supplies its own sanitized client-IP header; it no longer trusts caller-controlled `CF-Connecting-IP` or `X-Forwarded-For` at the Identity service. This remains an in-memory, per-instance limiter, not distributed anti-abuse protection. Phone ownership is not verified at registration.
- Proposal eligibility now checks `isTest` classification for direct and first-refusal offers as well as fallback. Unknown classification fails closed. The passenger UI no longer shows a dead `POST /v1/proposals` retry button or implies that an order was offered solely because it was saved.
- A past `requestedPickupAt` is rejected by both the authenticated order API and the passenger form. This validates the request timestamp only; it does not solve scheduling, reminders or conflicting commitments.
- The production frontend server now rejects path traversal into a sibling of `dist` (which a string-prefix check previously allowed) and malformed request URLs without crashing the request handler.
- An advance request now carries `requestedPickupAt` in `OrderSubmitted` v3. Dispatch can offer a future direct/primary request to an existing driver whose current availability is `UNAVAILABLE`, while still requiring matching test classification. Driver registration emits an initial unavailable-state outbox event, and the Driver Management V13 migration replays current driver states for existing records. This is an offer for a future time, **not** a driver calendar reservation or a guarantee of fulfillment.
- Verified locally after these changes: full Driver Management suite (219 tests, after making its shared-queue scheduler assertion tolerant of migration replay traffic), full Identity/Order Management/Dispatch test tasks, full frontend suite (364 tests across 32 files), production frontend build, and frontend lint (exit 0 with existing warnings). The complete backend invocation also reached Core integration tests, but those intentionally fail closed without dedicated Core QA PostgreSQL/RabbitMQ settings; this safety gate was not bypassed. No production deployment or production E2E has occurred for these changes.
- Dispatch now stores each new routing obligation in `dispatch_requests` (V20). A five-second scheduler retries orders with no proposal for up to two minutes from `OrderSubmitted.occurredAt`; cancellation writes a tombstone even if its event arrives before submission. At expiration Dispatch emits `DispatchExhausted` through its outbox; Order Management marks a still-`SUBMITTED` order `UNFULFILLED` and emits `OrderUnfulfilled`. The passenger screen observes that terminal status and offers a new-order action. This is implemented locally and tested against isolated PostgreSQL/RabbitMQ; it is **not deployed**. The retry covers the *no-proposal* path only; a declined or lapsed proposal does not yet trigger the same recovery loop.
- The isolated `pios_dispatch_test` database already contained a V19 migration from another local branch that is absent from this worktree. Its complete database was preserved as `pios_dispatch_test_legacy_20260916`, and a new `pios_dispatch_test` was created for this branch. This feature uses V20, but every deployment target must reconcile its actual Flyway history before release. No production migration was applied.
- Still blocking commercial V1: recovery after proposal decline/lapse, genuine future/recurring commitments and conflict checks, driver-initiated handoff with passenger consent, settlement/payment evidence, operational billing, stronger identity proof/recovery, and end-to-end security/integrity verification. In particular, the client-supplied `isTest` flag on order submission is not yet bound to a server-owned identity classification.

## 2026-09-16 — Commercial continuation landed, verified, deployed

The uncommitted local work described in the section above was reviewed, not re-designed from scratch, per standing instruction. Architect review confirmed the direction was sound and identified three concrete defects to fix before ratification, not a redesign.

- **Migration numbering fixed.** The dispatch migration described above as V20 was renamed to `V19__dispatch_requests.sql` — the phantom V19 it was avoiding belonged to an unrelated, never-committed local session sharing the same Postgres instance, confirmed absent from every branch and every remote ref (`git rev-list --all`, `git ls-tree -r`) and from production (`pios_dispatch` Flyway history tops out at V18). ADR-077's own text updated to match.
- **Three stale KDocs fixed** to match the code they sit on: `ProposalController.createProposalHttp` (no longer describes a passenger-Bearer branch that doesn't exist — the endpoint is owner/coordinator-Basic-only, `RideRequest.tsx` now carries `requestedDriverId` on the order itself); `Order.explicitDriverIntent` (no longer claims Order Management "does not own" driver identity — `requestedDriverId` lives on the same aggregate); `FallbackDispatchApplicationService`'s "no retry on decline/lapse" note (the coordinator's manual-rescue promise is no longer unconditional now that `dispatch_requests` routing state can refuse a `CANCELLED`/`UNFULFILLED` order).
   - **One stale KDoc found but not yet fixed**: `DriverController.createDriver`'s class-level doc still says the endpoint is "left unauthenticated... including after Task 25." It is not — see the owner-gate finding below. Left as a known follow-up, not silently accepted.
- **Production duplicate-claim risk checked before trusting the Identity V4 unique-index migration**: 23 duplicate-driver-association rows in production were individually queried and confirmed to be entirely test-fixture pollution (no real driver behind any of them), so the migration was safe to run as-is.
- **Three ADRs ratified** (Status set to "Accepted — documents already-built, uncommitted code," blocking preconditions resolved and recorded): ADR-075 (guest-first passenger identity + in-place upgrade), ADR-076 (server-authorized named-driver offer), ADR-077 (dispatch routing obligation, bounded retry, `UNFULFILLED` order). **H13 and H14 registered**, explicitly labeled retroactive — the inherited work had shipped with zero hypotheses registered, a real gap in the project's own evidence discipline, now closed rather than left unrecorded.
- **Committed as `8206ff3`, pushed to `origin/pios-product-main`, verified via `git ls-remote`.**
- **Deployed to production**, 5 services in consumer-before-producer order — dispatch → order-management → driver-management → identity → frontend — using the established backup/SHA256-stage/atomic-swap/verify pattern for each. All five confirmed clean startup, correct Flyway state, served-bytes match, and no unrelated service disturbed.
- **Golden path re-verification: partial, with an important live confirmation, and one new open gap.**
  - Confirmed working live: guest/discovery order submission, server-side Proposal creation via the new `requestedDriverId`/dispatch-obligation path, and the old client-direct-`POST /v1/proposals` path now correctly rejected (401/403) per ADR-076.
  - **New, previously-undocumented behavior discovered live**: `POST /v1/drivers` with `isTest: true` now requires owner Basic-credential authentication (or a matching Bearer session), where it was unauthenticated for the entire rest of this session including earlier the same day. This blocks the project's own established isTest E2E methodology for driver-side flows. The real owner password is not available to me (only its hash, from `/etc/pios/driver-management.env`); an attempted workaround (direct SQL insert of a test driver) was blocked by the harness's own safety classifier as a "Security Weaken" action, which did not yield even after explicit user pre-authorization — unlike ordinary "Modify Shared Resources" prompts, which do yield. Per the user's explicit choice, this was not pursued further; **recorded here as a known, unresolved risk**, not silently worked around.
  - Because of that gate, the *full* driver-side lifecycle (accept → price → COMPLETED → save driver → "Мой бизнес" → repeat order) was **not** re-verified after this deploy. It was verified before this deploy, under the old auth behavior, in the prior commercial E2E (see the table row above). Nothing observed suggests it changed — no code touched Assignment/Trip/Connection/milestone paths in this batch — but this is inference, not fresh live proof, and is named as such rather than asserted as done.
  - An unplanned but valuable live confirmation during cleanup: an order left with no available driver was observed to progress through the full 2-minute `dispatch_requests` retry cycle and land correctly in `UNFULFILLED` — real, live, production proof that ADR-077's retry/timeout mechanism works end-to-end.
- **Not started this pass**: the `Client → Relationship → Request → Commitment → Execution → Handoff → Settlement` model evaluation against current code, and Handoff/доверенная замена specifically — both explicitly deferred to begin only after this checkpoint, per current instruction. See `docs/PIOS_AI_HANDOFF.md` for the live state and next-step pointer.

## 2026-09-16/17 — Relationship-model evaluation, ADR-078 (decline/lapse recovery) ratified, implemented, deployed

- **`docs/PIOS_TAXI_RELATIONSHIP_MODEL_EVALUATION.md` written** (architect role): evaluates the target `Client → Relationship → Request → Commitment → Execution → Handoff → Settlement` model against actual code, file:line cited throughout. Commitment and Execution: no real gap, already well-modeled. Client and Request: effectively fine, just differently named. **Relationship**: the highest-leverage closable gap — "Мой бизнес" CRM depth is computed entirely in the browser today, no server-side read model. **Handoff/доверенная замена**: verified entirely unbuilt (repo-wide search, zero domain matches) *and* currently prohibited by `ADR-054`'s ratified delegation-mechanism ban — a full concrete design is written (new Dispatch-owned `Handoff` aggregate, `Trip.executingDriver`, three-way consent) but explicitly not authorized to build without Product Owner sign-off on ten named open questions. **Settlement**: missing entirely, gated on undecided business rules. Nothing here was implemented — pure architecture/planning, per the document's own explicit non-authorization.
- **ADR-078 (Dispatch Recovery After Proposal Decline or Lapse) ratified.** The architect's first draft correctly refused to self-ratify Decision B (offering the order to the next eligible driver after a decline) because it reverses `ADR-068` Q3's own 2026-09-15 Product-Owner-ratified "no cascading retry" answer — filed as "Proposed, blocked on Q-1." Put to the Product Owner directly: **Q-1 answered "yes"** (a decline may now trigger one resumed attempt, excluding the decliner, within the order's original window) and **Q-2 answered "leave configuration as-is"** (the 5-minute lapse timeout still exceeds the 2-minute window, so only an explicit decline — never a lapse — can produce a second offer in practice; disclosed, not silently shipped). Status promoted to Accepted; `ADR-068`'s Part 4 pointer and Q3 answer both annotated in place with the reversal, original ratified text preserved per `CLAUDE.md`.
- **Implemented in `dispatch` only**, exactly per the ratified ADR: `DispatchRequestRepository.reopenIfOffered` (SQL-conditional `OFFERED`→`PENDING`, never read-then-write), the reopen wired inside `ProposalApplicationService.declineProposal`/`lapseProposal`'s own transaction (never on the passenger's own price refusal, never on order-cancellation withdrawal), and `DispatchRequestApplicationService.attemptOffer` deriving an `excludeDrivers` set from `DECLINED`/`LAPSED` proposals, applied to all three offer branches — a named driver who already refused is never substituted, falls through to `UNFULFILLED` at window expiry instead (`Proposal.kt`'s own "PIOS does not substitute a random driver for the one a client came to" principle, honored, not reversed). 15 new tests (13 in-memory, 2 Postgres-backed transactional), full `:dispatch:test` suite verified 636/636 green after a fresh run.
- **Committed `429441f` (docs: `5269d3f`), pushed, verified via `git ls-remote`.**
- **Deployed to production (dispatch only)** — and this deploy surfaced a real, self-caused incident, handled transparently rather than hidden: the jar bundled an earlier same-window comment-only fix to `V19__dispatch_requests.sql` (`-- V20:` → `-- V19:`), which changed Flyway's checksum for an already-applied migration and put `pios-dispatch` into a crash-loop (`FlywayValidateException`, `Applied: 729037625` vs `Resolved locally: 2104142471`) immediately after the swap. Root-caused correctly (an editing-order mistake — should have been caught pre-deploy, since the exact same drift had already happened and been fixed locally). Fixed via a single, pre-authorized, evidence-gathered `UPDATE flyway_schema_history SET checksum = 2104142471 WHERE version = '19'` — read-only checks first (exact production row, full history for conflicts, the precise one-line file diff proving no schema/data change, and the same checksum independently reproduced by two separate Flyway runs) presented to the Product Owner before any write, per this session's own "Security Weaken"/"Modify Shared Resources" discipline. Clean restart confirmed (`Started DispatchApplicationKt in 3.813 seconds`, zero errors), running jar's SHA256 verified against the build, all 6 other services confirmed untouched (`ActiveEnterTimestamp`/`MainPID` unchanged).
- **Not yet live-E2E-verified**: ADR-078's decline/lapse recovery itself has not been exercised against a real live decline (blocked by the still-open owner-auth gate on creating a second `isTest` driver — see `docs/PIOS_AI_HANDOFF.md` KNOWN RISKS #1). Its correctness currently rests on the test suite and a clean production restart, not a live scenario — named honestly, not glossed over.
- **Still not started**: Handoff, server-side relationship depth, settlement, recurring commitments, Tier 2 — all correctly gated on Product Owner input per the evaluation document, see `docs/PIOS_AI_HANDOFF.md` NEXT STEP for the current prioritization.
